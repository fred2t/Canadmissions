// import ReCAPTCHA from "react-google-recaptcha";

// import { useAppDispatch } from "../../redux/hooks";
// import { setReCAPTCHA } from "../../redux/slices/clientSlice";

export default function AppReCAPTCHA(): JSX.Element {
    // const dispatch = useAppDispatch();

    // const onReCAPTCHA = (reCAPTCHA: ReCAPTCHA) => {
    //     if (reCAPTCHA == null) return;

    //     dispatch(setReCAPTCHA(reCAPTCHA));
    // };

    return (
        <></>
        // <ReCAPTCHA
        //     sitekey={process.env.NEXT_PUBLIC_API_RECAPTCHA_SITE_KEY as string}
        //     size="invisible"
        //     ref={onReCAPTCHA}
        // />
    );
}
